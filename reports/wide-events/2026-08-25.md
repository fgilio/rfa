# Wide Events Audit: 2026-08-25

## Summary

- Deterministic checks: pass
- Findings: 0 critical, 0 warning, 1 info
- Files reviewed: 15

## Deterministic Checks

| Command | Result | Notes |
|---|---|---|
| `php artisan test --compact tests/Arch/LoggingConventionsTest.php` | pass | 9/9. C1-C7 and C9-C10 hold on `main` at `c17b6d7`. |
| `php artisan test --compact tests/Feature/LogChannelPostureTest.php` | pass | 5/5. Resolved default channel and stack stay local (C8). |
| Production facade inventory (command below) | pass | 143 hits across 15 production files. No hits in `config/` (only `config/logging.php` was in scope and does not call the facades). |

```bash
rg -n "Log::(debug|info|warning|error|critical)\(|Context::(flush|add)\(" app resources/views config -g'*.php'
```

## Findings

### [INFO] `OpenProjectFromPathAction` warning duplicates fields already on both owner canonical events

- **File:** `app/Actions/OpenProjectFromPathAction.php:50`
- **Rule:** Agent Review Checklist A12, "Diagnostic detail uses safe fields ... and the chosen fields actually aid triage, not just satisfy the linter."
- **Evidence:** The `Throwable` branch adds `rfa.reason = project_registration_failed`, `rfa.error_class = $e::class`, and returns null. It also emits `Log::warning('project.registration.failed', ['reason' => 'project_registration_failed', 'path_hash' => hash('xxh128', $path), 'error_class' => $e::class])`. Both current callers already carry those exact three values on their owner canonical events: `HandleDeepLink` writes `rfa.path_hash` (same `xxh128` hash) and `rfa.reason` / `rfa.error_class` into `deeplink.opened`, and `NativeAppServiceProvider::processInbox` writes `rfa.path_hash`, `rfa.reason`, `rfa.error_class` into `inbox.opened`.
- **Impact:** Low. The warning gives triage no new field beyond what the owner already logs, so an operator sees the same failure twice per event. The previous audit surfaced this as a residual risk and left the warning in place as insurance against a future caller arriving without an owner.
- **Suggested fix:** Optional. Either remove the `Log::warning` (rely on the owner's canonical event) or add a safe diagnostic field that neither owner event carries. Both callers cover the failure today, so removal is the cleaner path if `OpenProjectFromPathAction`'s callers remain owner-backed.

## Clean Areas

- Every canonical `Log::info()` has one clear owner in the correct layer: `HandleMenuItemClicked` for `menu.item.clicked`, `HandleDeepLink` for `deeplink.opened`, four inline updater listeners in `NativeAppServiceProvider` for `updater.available` / `updater.current` / `updater.downloaded` / `updater.failed`, `NativeAppServiceProvider::processInbox` for `inbox.opened`, `add-project-menu.blade.php` for `project.opened` and `directory.scanned`, `context-page.blade.php` for `context.comment.written`, and `review-page.blade.php` for `review.refreshed`. No child action or service duplicates a parent canonical info event.
- `Context::flush()` runs first in every canonical owner. Child services and actions (`OpenProjectFromPathAction`, `OpenRepositoryDialogAction`, `OpenTerminalRequestAction`, `GitMetadataService`, `SyntaxHighlightService`, `ResolveBranchBaseAction`, `LoadFileDiffAction`, `GetProjectStatusAction`, `EnsureRfaGitExcludeAction`) add `rfa.reason` / `rfa.error_class` to Context or emit inline-payload warnings without flushing, preserving owner correlation.
- Every canonical event carries `rfa.outcome` and `rfa.duration_ms`. Context is rich enough to reconstruct each operation without reading code: `rfa.repos_found` / `rfa.repos_registered` / `rfa.repos_already_tracked` / `rfa.repos_failed` on scan, `rfa.file_count_before` / `_after` and `rfa.changed_count` / `rfa.changed_file_ids` on review refresh, `rfa.project_slug` / `rfa.file_id` / `rfa.is_draft` on comment writes, `rfa.mode` / `rfa.path_hash` / `rfa.request_id` / `rfa.route` on deep-link and inbox opens, `rfa.update_version` on updater outcomes.
- `rfa.outcome` values follow the vocabulary and the decision tree. `deeplink.opened` uses `rejected` for invalid input, `skipped` for `request_already_claimed`, and `error` for both a handled `project_registration_failed` and a propagated exception recorded as `deeplink_open_failed`. `handleScanDirectory` correctly distinguishes `cancelled` (dialog dismissed), `partial` (some repos failed), and `completed`. `HandleMenuItemClicked` defaults to `skipped` for unknown menu ids, maps dialog cancellation and rejection separately, and uses `error` for both a handled `project_registration_failed` and a propagated exception recorded as `menu_click_failed`. `inbox.opened` reaches `skipped` via `request_already_claimed`, `rejected` via invalid inbox input, and `error` from either a handled `project_registration_failed` or the outer catch recorded as `inbox_open_failed`.
- `updater.failed` follows the listener-owned unit-of-work pattern: a sanitized diagnostic `Log::error` inside `try`, followed by a canonical `Log::info('updater.failed')` from `finally` with `rfa.outcome = error`. That keeps the failure path the same shape as the three success paths, so `rfa.outcome` distribution stays queryable across all four updater outcomes.
- Warning payloads consistently pair `reason` with safe diagnostic fields. `git.diff.failed`, `project.status.failed` carry `exit_code` and `stderr_summary` (via `LogSanitizer::summary`). The seven `git.*.failed` warnings in `GitMetadataService` carry `exit_code` and the relevant ref/hash/path (relative). `git.branch_base.resolve_failed` carries `base_branch`, `stage`, and `exit_code`. `git.exclude.*` warnings carry `error_class`. `context.comment.rejected` carries `reason` (from the enum), `file_id`, `side`, `start_line`, `end_line`. No payload passes `$e->getMessage()`, `$e->getTraceAsString()`, or raw `$e->stderr`.
- No banned absolute-path key appears in any static `Context::add()` call. `rfa.path_hash` is the `xxh128` hash of an absolute path, never the path itself. Warning-payload path values are relative (`LoadFileDiffAction`, `GitMetadataService::getFileContent`).
- Storage stays local. `config/logging.php` resolves `default` to `daily` with `LOG_DAILY_DAYS` retention and `info` level. The `slack`, `papertrail`, `syslog`, `errorlog`, and Monolog `stderr` channel definitions remain inert unless env overrides pull them in. `tests/Feature/LogChannelPostureTest.php` guards this by walking the resolved default channel and any stack members.
- No production `Log::debug()`. No `Log::info()` passes a manual context array. No static `Context` key lacks the `rfa.` prefix. All static `rfa.outcome` literals belong to the approved vocabulary.

## Residual Risks

- The production logging surface changed between audits. The previous audit's deterministic checks ran at `7e841bc`; this audit ran at `c17b6d7`. Seven production logging files changed between those commits. The changes added the `project.opened`, `directory.scanned`, and `inbox.opened` owners and updated the deep-link and menu outcome paths. This audit reviewed those owners and paths for the first time.
- `LoadFileDiffAction` runs per-file on lazy render and does not emit a canonical info event. Prior audits classified this as a scope judgment (per-file diff render is not an externally meaningful user operation). If it fails, the `git.diff.failed` warning inherits whatever Context the last canonical owner left behind, which may or may not still describe the project being viewed. Left unchanged, consistent with the 2026-08-11 and 2026-08-18 dispositions.
- Agentic review reads static source, not runtime traffic. Dynamic `Context::add()` values (e.g. `rfa.reason` set from an exception's `reason->value` enum on `context.comment.rejected`) are only spot-checked against the vocabulary. A future enum entry could drift from the approved outcome set without arch tests catching it, though outcomes there are `reason` fields on Context, not `rfa.outcome`, so the C6 constraint does not apply.
- `OpenRepositoryDialogAction::handle` still passes `$e->getMessage()` into `Alert::show()` for the user-facing error dialog. This is a UI surface, not a `Log::` or `Context::` payload, so it is outside the SKILL's raw-exception ban, but the exception text still leaves the app boundary through the dialog.
- The audit does not run the app or replay JSONL diagnostics, so runtime-only regressions (e.g. a listener that never reaches `finally` because Electron kills the PHP process mid-callback) stay outside its detection surface.
- Two entry points for "open a repository" emit different canonical event names (`menu.item.clicked` with `rfa.menu_id = 'open-repo'` from the native menu, `project.opened` from the in-app button). This is a design choice from the 2026-08-18 audit resolution and each entry point retains a clean owner, but the split makes a single "how often did a user open a project" query cross two event names. Not a rule violation.
