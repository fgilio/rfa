pre-commit:
  commands:
    lint:
      root: src/
      run: ./vendor/bin/pint --test

pre-push:
  commands:
    types:
      root: src/
      run: php -d memory_limit=512M ./vendor/bin/phpstan analyse
